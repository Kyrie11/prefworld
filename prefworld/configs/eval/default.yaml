dataset:
  cache_dir: /path/to/cache/prefworld
  split: val
  max_samples: null

model:
  agent_feat_dim: 7
  z_dim: 8
  pc_hidden: 64
  intent_hidden: 128
  energy_hidden: 128
  eb_temperature: 1.0
  eb_max_candidates: 64

eval:
  batch_size: 64
  num_workers: 4
  checkpoint: /path/to/checkpoints/best.pt
